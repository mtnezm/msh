#!/usr/bin/env bash

#
# These arrays contain the stuff that will be loaded into your shell.
# Make sure that the corresponding files exist!
#

#
# Available aliases can be found under ${MSH_ALIASES}
#

aliases=(
grep
ls
)

#
# Available completions can be found under ${MSH_COMPLETIONS}
#

completions=(
github
ssh
)

#
# Available plugins can be found under ${MSH_PLUGINS}
#

plugins=(
alias-completion
ansible
progress
)

#
# Available profiles can be found under ${MSH_PROFILES}
#

profiles=(
default
)
